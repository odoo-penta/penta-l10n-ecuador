<?xml version="1.0" encoding="utf-8"?>
<odoo>

  <record id="view_account_payment_custom_form" model="ir.ui.view">
      <field name="name">account.payment.custom.form</field>
      <field name="model">account.payment</field>
      <field name="inherit_id" ref="account.view_account_payment_form"/>
      <field name="arch" type="xml">
          <xpath expr="//group[@name='group2']" position='inside'>
              <separator/>
                <field name="bank_reference" invisible="not show_bank"/>
                <field name="used_card_id"
                    domain="[('active','=',True), ('company_id','in',[False, company_id])]"
                    options="{'no_create': True, 'no_open': True}"
                    invisible="not show_bank"/>
                <field name="card_payment_type" invisible="not has_used_card"/>
                <field name="number_months" invisible="not show_bank"/>
                <field name="number_lot" invisible="not show_bank"/>
                <field name="authorization_number" invisible="not show_bank"/>
                <field name="bank_id"
                    options="{'no_create': True, 'no_open': True}"
                    invisible="not show_bank"/>
          </xpath>
      </field>
    </record>

</odoo>
