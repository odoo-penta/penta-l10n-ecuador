<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Vista list para account.cards -->
    <record id="view_account_cards_list" model="ir.ui.view">
        <field name="name">account.cards.list</field>
        <field name="model">account.cards</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <!-- Vista form para account.cards -->
    <record id="view_account_cards_form" model="ir.ui.view">
        <field name="name">account.cards.form</field>
        <field name="model">account.cards</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="active"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
        <record id="action_account_cards_list" model="ir.actions.act_window">
        <field name="name">Tarjetas de Crédito</field>
        <field name="res_model">account.cards</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_account_cards_list"/>
        <field name="target">current</field>
    </record>

    <menuitem id="menu_account_cards_action"
          name="Tarjetas de Crédito"
          parent="account.menu_finance_configuration"
          action="action_account_cards_list"
          sequence="101"/>
</odoo>
